# Frontend environment variables
# Copy this file to `.env` and fill in values.

# Stripe publishable key used by `loadStripe` in CheckoutFlow
# This MUST start with `pk_test_` or `pk_live_` and be valid.
# Example: pk_test_51Nxxxxxx... (from your Stripe dashboard)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Optional: API base URL if not using CRA proxy
# REACT_APP_API_BASE_URL=http://localhost:5000